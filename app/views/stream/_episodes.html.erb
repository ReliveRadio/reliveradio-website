<% if !@episodes.blank? %>
	<!-- SHOW ALL UPCOMING PODCASTS -->
	<% @episodes.each_with_index do |episode, index| %>
		<% podcast = episode["db"] %>
		<%= render 'single_episode', :podcast => podcast, :episode => episode, :index => index %>
	<% end %>
<% end %>